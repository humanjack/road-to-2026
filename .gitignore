node_modules
dist
.DS_Store
*.local
.vite
coverage

# Makefile runtime state (pid + log)
.run/

# QA / test artifacts
slice*.png
.playwright-mcp/

# local reference material (videos/images), not project assets
references/
